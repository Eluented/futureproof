const app = require('./app');

const port = 3000;

app.listen(port, ()=>{
    console.log(`Express example app listening at http://localhost:${port}`); // just to see if everything works fine - listening ? idk
});